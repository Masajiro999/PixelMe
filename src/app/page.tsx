import { PixelMeClient } from '@/components/pixel-me-client';

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-4 md:p-8">
      <PixelMeClient />
    </main>
  );
}
